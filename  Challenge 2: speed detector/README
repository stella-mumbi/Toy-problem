# Speed Demerit Points Calculator

This program takes the speed of a car as input and calculates demerit points based on the specified criteria. If the speed is less than 70, it prints "Ok". Otherwise, for every 5 km/s above the speed limit (70), it gives the driver one demerit point and prints the total number of demerit points.

## Files

- **index.html**: Contains the HTML structure for the speed demerit points calculator. Links to the JavaScript file and the external CSS file.

- **script.js**: Contains the JavaScript code for calculating and displaying demerit points.

- **styles.css**: External CSS file with styles for the HTML elements.

## Usage

1. Open the `index.html` file in a web browser.
2. The program will calculate and display demerit points based on the provided speed.

## Styling

Styling is applied through the `styles.css` file